# Node.js 应用端口
PORT=8000

# 数据库连接字符串 (Northflank MySQL)
# 示例: mysql://user:password@host:port/database?sslmode=REQUIRED
# Northflank 会自动注入 MYSQL_CONNECTOR_URI 环境变量，通常无需手动配置
# 如果需要本地测试，请替换为你的 MySQL 连接字符串
DATABASE_URL="mysql://user:password@host:3306/database?sslmode=REQUIRED"

# JWT 密钥 (请务必修改为强随机字符串)
SECRET_KEY="your-super-secret-key-please-change-this-in-production"

# Xray API 配置
# Xray API 的 gRPC 端口，通常是 8080 或 10085
# 如果 Xray 提供了 HTTP JSON API，请修改为对应的 URL
XRAY_API_URL="http://localhost:8080/stats"
# 如果 Xray API 需要认证，请提供 API 密钥
# XRAY_API_KEY="your-xray-api-key"
